# 핸드북 콘텐츠 작성 스타일 가이드

> 업계 선배가 신입 후배들이 자기가 했던 고생을 하지 않았으면 하는 마음에서 만들어둔 블로그.

이 한 문장이 이 프로젝트의 글쓰기 톤을 정의합니다. 아래는 그 톤을 일관되게 유지하기 위한 구체적 규칙입니다.

## 1. 챕터 구조

모든 챕터는 아래 순서를 따릅니다.

```
# [번호] [주제] — [부제]

## 이 챕터에서 배우는 것
- (3~5개 학습 목표)

---

## 본문 섹션 (H2/H3)
(핵심 내용)

---

## 핵심 정리
(요약)

---

*다음 챕터: [번호] [제목]*
```

## 2. 제목 형식

- H1: `# 1.1 주제 — 부제` (번호 + 주제 + em-dash + 실용적 각도의 부제)
- H2: 본문의 논리 단위. 질문형(`왜 실리콘인가`) 또는 명사형(`트랜지스터의 구조`)
- H3: H2 안의 세부 항목

## 3. 문체와 톤

- **경어체 기반**: "~이다", "~한다" 등 평서형 종결. 존댓말("~입니다")과 혼용 가능하되, 챕터 내에서 일관성 유지
- **간결한 문장**: 한 문장에 하나의 아이디어. 불필요한 수식어 배제
- **능동태 우선**: "~된다"보다 "~한다"를 선호
- **직접 호칭**: "우리는", "CS 전공자라면" 등으로 독자를 자연스럽게 포함
- **수사적 질문**: 섹션 전환이나 개념 도입 시 질문으로 시작 가능 ("왜 이렇게까지 해야 하냐면...")

## 4. 용어 도입

새로운 전문 용어를 처음 소개할 때:

- **굵은 한국어 + (영어)** 형식: `**반도체(Semiconductor)**`
- 바로 뒤에 한 줄 정의: `**반도체(Semiconductor)**는 말 그대로 "반쯤 전기가 통하는 물질"이다.`
- 이후 재등장 시에는 굵게 처리하지 않고 자연스럽게 사용
- 약어는 첫 등장 시 풀네임 병기: `**화학기상증착(Chemical Vapor Deposition, CVD)**`

## 5. 비유와 연결

- 독자(AI/ML 엔지니어)가 이미 아는 개념에 매핑하여 설명
- 소프트웨어 아키텍처, 데이터 구조, ML 워크플로우 등에서 비유를 가져옴
- 비유 후 반드시 한계를 명시하거나 정확한 설명으로 보충

**좋은 예시**:
> "반도체는 일종의 **하드웨어 레벨의 if문**이다. 조건(전압)에 따라 전류가 흐르거나(True) 흐르지 않거나(False)."

> "팹은 **하이퍼스케일 데이터센터의 물리적 버전**이다. 막대한 초기 투자(CapEx), 극도의 환경 제어, 가동률과 효율이 비즈니스의 생명선이라는 점에서 놀라울 정도로 닮아 있다."

> "전체 공정 흐름은 **중첩된 for 루프**와 같다."

## 6. 수식

- 직관적 설명을 **먼저** 제시한 뒤 수식을 보여줌
- 수식 뒤에 각 변수의 의미를 한국어로 풀어서 설명
- 유도 과정이 필요하면 `<details>` 접기로 분리
- KaTeX 문법 사용 (`$$...$$` 블록, `$...$` 인라인)

**패턴**:
```
[직관적 설명]

$$R = k_1 \times \frac{\lambda}{NA}$$

여기서 $R$은 최소 패턴 크기, $\lambda$는 광원 파장, $NA$는 개구수이다.
```

## 7. 코드 예시

- 반도체 핸드북: 개념 설명이 중심이므로 코드는 최소화
- 통계학 핸드북: 매 챕터 "Python으로 확인하기" 섹션에 실행 가능한 코드 포함
  - Python + numpy/scipy/matplotlib
  - 3~15줄의 짧은 스니펫
  - 변수명은 영어, 주석은 한국어
  - 개념의 **증명 by 실행** 목적

## 8. 강조와 콜아웃

| 용도 | 형식 | 색 |
|------|------|---|
| 개념적 보충 | `> 💡 **제목**: 설명` | 시그니처 그린 `#00d4a4` |
| 옵션·기술 메모 | `> ⚙️ **제목**: 설명` | 중립 회색 `#888888` |
| 주의사항 | `> ⚠️ **제목**: 설명` | warn 앰버 `#c37d0d` |
| 심화 내용 | `<details>`<br>`<summary>🔬 Deep Dive — 제목</summary>`<br>`> 본문...`<br>`</details>` | (본문 blockquote 는 기본 중립 톤; 안의 💡/⚠️/⚙️ 는 종류 색) |
| 올바른 해석 | `✅ "올바른 해석"` | (인라인) |
| 잘못된 해석 | `❌ **"잘못된 해석"**` | (인라인) |

> 색은 _MarkdownViewer 의 blockquote 인터셉터_ 가 _이모지 첫 글자_ 를 인식해 `data-variant` 로 분기 (`globals.css` 의 `.prose blockquote[data-variant="..."]`). 작성 시 별도 마크업 _불필요_ — _이모지가 종류 신호_ 자체.

**시각적 위계와 접기 기준**:

- 🔬 **Deep Dive** — 항상 `<details>` 로 접기. 본문 흐름을 끊지 않으면서 _클릭해서 펼치는_ 깊은 보충.
- 💡 / ⚙️ / ⚠️ — 펼친 채 blockquote. 1~3줄 짧은 보충이 기본. 독자가 _즉시 보고 다음으로 진행_ 하는 자리.
- 콜아웃이 _6줄 이상_ 으로 길어지거나 _코드 블록·표를 포함_ 하면 `<details>` 도입 검토. 다만 _독자가 본문에서 즉시 봐야 할 정보_ (예: "두 가지 디테일을 미리 짚어둔다") 는 길어도 펼친 채 두는 게 낫다.

## 9. 문단 리듬

- **짧은 선언문** (1~2문장): 핵심 정의, 전환 ("문제는 그 다음이다.")
- **중간 설명** (3~4문장): 개념 풀어쓰기
- **긴 서술** (5문장 이상): 비유 전개, 구체적 수치를 곁들인 사례
- 세 가지를 의도적으로 섞어서 단조로움 방지

## 10. 목록 vs 산문

- **목록 사용**: 구성 요소 나열, 공정 단계, 비교표, 핵심 정리
- **산문 사용**: 개념 설명, 비유 전개, "왜?"에 대한 답변, 섹션 간 연결
- 목록이 3개 이하이면 산문으로 풀어쓰는 것을 선호

## 11. 실무 연결

- 구체적 수치와 규모감 제공 (비용, 시간, 수량)
- 실제 장비명, 기업명, 프레임워크명 언급
- "AI 엔지니어에게 이것이 중요한 이유"를 명시적으로 서술

## 12. 핵심 정리 (챕터 마무리)

- 3~5개 핵심 포인트를 글머리 기호로 정리
- 각 포인트는 **굵은 한 줄 요약** + 부연 설명
- 암기가 아니라 "이 챕터의 결론이 당신의 업무에 어떤 의미인지"를 전달

## 13. 챕터 간 연결

- 챕터 시작 시 이전 챕터의 핵심을 한 문장으로 요약하여 연결
- 챕터 끝에 다음 챕터 티저
- 다른 챕터의 개념을 참조할 때 "N.M장에서 다룬 X"처럼 명시
